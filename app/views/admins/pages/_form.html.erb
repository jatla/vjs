<%= form_for(@admins_page) do |f| %>
  <% if @admins_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admins_page.errors.count, "error") %> prohibited this admins_page from being saved:</h2>

      <ul>
      <% @admins_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name%>
  </div>

  <div class="textarea">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
